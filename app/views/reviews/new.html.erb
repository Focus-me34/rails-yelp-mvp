<div style="width: 600px; margin: 20px auto">
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: 1..5 %>
    <%= f.submit "Post review", :class => "create" %>
  <% end %>
</div>
